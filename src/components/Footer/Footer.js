import React from "react";
import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <h2> react is very fun to the learn @2021</h2>
    </footer>
  );
};

export default Footer;
